# pandas 章节续工记录

更新时间：2026-05-30

## 当前停点

上一轮已提交到 `5a3a32d Refine pandas chapter progression`。当前讲义可运行，但数据设计还需要继续调整。

当前问题：阶段 4 仍然是在讲义里临时构造脏数据，教学上不够自然。后续准备改成预先制作脏数据表，直接读入后检查和清洗。

## 已做

- 新 pandas 章节已放在 `新章节编辑/pandas/`。
- 主讲义由 `build_pandas_finance_to_analysis.py` 生成 `pandas_finance_to_analysis.ipynb`。
- 已按“阶段目标 + 知识点簇 + 范例任务”重写阶段 1-5 的主要内容。
- 已补入小节改写规则：`小节修改准则.md`。
- 已加入外部审查意见：`pandas_finance_to_analysis_ds4检查.md`，暂未处理。

## 下一步思路

准备把原来的两表设计改成三表设计：

1. 表 1：干净财务主表
   - 用于阶段 1-3。
   - 讲读取、查看、选行选列、筛选、列运算、新变量、排序、排名。
   - 保留 `证券代码`，但前期可以少展示或暂时不处理。

2. 表 2：脏财务指标表
   - 用于阶段 4。
   - 和表 1 通过 `证券代码 + 年份` 对应。
   - 预先放入自然脏问题，例如前导 0、空格、百分号文本、`--`、带逗号数字、缺失值、重复公司年份、非法日期、空字符串、异常比例等。
   - 阶段 4 直接读入这张表，检查并清洗，不再现场造脏数据。

3. 表 3：干净公司背景表
   - 用于阶段 5。
   - 和财务表通过 `证券代码` 合并。
   - 字段包括行业、地区、上市市场、上市日期等。

阶段 5 目标改为连续合并：

```python
analysis_df = (
    finance_statement_clean
    .merge(finance_metrics_clean, on=["证券代码", "年份"], how="left")
    .merge(company_profile_clean, on="证券代码", how="left")
)
```

后续阶段 6-8 用合并后的 `analysis_df` 继续做分组、重塑和时间序列。
